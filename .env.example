# Copia este fichero como .env y rellena los valores reales.
# El fichero .env NUNCA debe commitearse (ya está en .gitignore).

# Clave de API de Groq — necesaria únicamente para las celdas LLM del notebook 05.
# Obtén una nueva en https://console.groq.com/keys
GROQ_API_KEY=
